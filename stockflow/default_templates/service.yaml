apiVersion: v1
kind: Service
metadata:
  name: {{ include "stockflow.fullname" . }}
  labels:
    {{- include "stockflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stockflow.selectorLabels" . | nindent 4 }}
